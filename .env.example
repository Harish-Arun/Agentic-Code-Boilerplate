# Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# GENERAL
# ============================================
ENVIRONMENT=development
LOG_LEVEL=INFO
DEBUG=true
DATA_DIR=./data

# ============================================
# DATABASE (Pluggable)
# Options: sqlite, postgres, mongo
# ============================================
DATABASE_TYPE=sqlite
DATABASE_PATH=./data/nnp_ai.db

# Postgres (if DATABASE_TYPE=postgres)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=nnp_ai
# POSTGRES_USER=admin
# POSTGRES_PASSWORD=changeme

# MongoDB (if DATABASE_TYPE=mongo)
# MONGO_URI=mongodb://localhost:27017/nnp_ai

# ============================================
# LLM PROVIDER (Pluggable)
# Options: mock, gemini, openai, azure
# ============================================
LLM_PROVIDER=mock

# Gemini (if LLM_PROVIDER=gemini)
# GEMINI_API_KEY=your-api-key
# GEMINI_MODEL=gemini-1.5-pro

# OpenAI (if LLM_PROVIDER=openai)
# OPENAI_API_KEY=your-api-key
# OPENAI_MODEL=gpt-4

# Azure (if LLM_PROVIDER=azure)
# AZURE_OPENAI_ENDPOINT=https://xxx.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-api-key
# AZURE_OPENAI_DEPLOYMENT=your-deployment

# ============================================
# SERVICE URLS (Docker internal)
# ============================================
API_SERVICE_URL=http://api-service:8000
AGENTS_SERVICE_URL=http://agents:8001
MCP_SERVICE_URL=http://mcp-tools:8002
FRONTEND_URL=http://localhost:3000

# ============================================
# SERVICE PORTS (External)
# ============================================
API_PORT=8000
AGENTS_PORT=8001
MCP_PORT=8002
FRONTEND_PORT=3000

# ============================================
# REDIS (Optional - for caching/state)
# ============================================
REDIS_ENABLED=false
REDIS_URL=redis://redis:6379/0
